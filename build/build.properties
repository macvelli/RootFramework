#
# This is the Ant build.properties file for the RootFramework project
#
app.name=root
version=0.5

src=${basedir}/src
lib=${basedir}/lib
build=${basedir}/build

classdir=${build}/classes
distdir=${build}/dist

jarFile=${app.name}-${version}.jar
extFile=${app.name}-${version}-ext.jar
srcFile=${app.name}-${version}-src.zip
